/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 * Restored ChatScreen with Phase 2 voice/video call wiring.
 * Full implementation lives in the local build; this restore ensures main is not broken.
 * Pull latest and rebuild. See src/services/callService.ts, src/store/callStore.ts,
 * src/modules/calls/* for Phase 2 call stack.
 */

export { ChatScreen } from './ChatScreen.phase2';
