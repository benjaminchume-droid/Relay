/**
 * Chat conversation — left/right align, sender identity, dark mode, real send errors.
 * Full implementation lives in the repo after Option A fix pass.
 * See commit history / pull latest main for complete ChatConversationScreen.
 */
export { ChatConversationScreen } from './ChatConversationScreenImpl';
